# AUTOCAMB.IT

Sito web moderno per la vendita di auto usate in Sardegna, costruito con Next.js 14, React e Tailwind CSS.

## Caratteristiche

- 🚗 Lista auto usate con filtri avanzati
- 🔍 Ricerca per marca, modello e versione
- 📱 Design responsive e moderno
- ⚡ Performance ottimizzate con Next.js
- 🎨 UI/UX accattivante con Tailwind CSS

## Tecnologie

- **Next.js 14** - Framework React con App Router
- **TypeScript** - Type safety
- **Tailwind CSS** - Styling utility-first
- **Lucide React** - Icone moderne

## Installazione

```bash
# Installa le dipendenze
npm install

# Avvia il server di sviluppo
npm run dev
```

Apri [http://localhost:3000](http://localhost:3000) nel browser.

## Scripts

- `npm run dev` - Avvia il server di sviluppo
- `npm run build` - Crea la build di produzione
- `npm start` - Avvia il server di produzione
- `npm run lint` - Esegue il linter

## Struttura del progetto

```
autocamb/
├── app/              # App Router di Next.js
│   ├── auto/         # Pagine auto
│   ├── layout.tsx    # Layout principale
│   └── page.tsx      # Homepage
├── components/       # Componenti React
├── data/            # Dati mock
├── types/           # TypeScript types
└── public/          # File statici
```

## Licenza

Tutti i diritti riservati © 2024 AUTOCAMB.IT

